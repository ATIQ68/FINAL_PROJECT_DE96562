To generate the ultrasound, the Trig must be set to a high state of 10 µs.  This will send an 8-cycle sonic burst which will move at speed, sound and it will be received into the echo pin.  The echo pin shows the time in microseconds of the waveform.

Using ultrasonic sensor and Arduino distance measurement consists of a transmitter part of ultrasonic module units Ultrasonic high-frequency waves in the formulation of programs after interference of these items with any object, these items are used to measure the distance from any object detected by measurement time taken by these items from transmitter and the receiver.

#include <LiquidCrystal.h> // includes the LiquidCrystal Library

const int rs=12, en=11, d4=5, d5=4, d6=3, d7=2 ;
LiquidCrystal lcd(rs,en,d4,d5,d6,d7); // Creates an LCD object. Parameters: (rs, enable, d4, d5, d6, d7)
const int trigPin = 10;
const int echoPin = 13;
long duration;//LONG VARIABLE
int distanceCm, distanceInch;
//declaration of all pin we used

void setup(){
lcd.begin(16,2); // Initializes the interface to the LCD screen, and specifies the dimensions (width and height) of the display
lcd.print("  WELCOME ATIQ  ");
lcd.setCursor(0,1);//FOR SECOND LINE OF LCD
lcd.print("FINAL PROJECT");
delay(5);
pinMode(trigPin, OUTPUT);
pinMode(echoPin, INPUT);
Serial.begin(9600);
}


void loop() {
digitalWrite(trigPin, LOW);
delay(2);
digitalWrite(trigPin, HIGH);
delay(10);
digitalWrite(trigPin, LOW);
duration = pulseIn(echoPin, HIGH);
distanceCm= duration*0.0344/2;
distanceInch = duration*0.00034/2;
lcd.setCursor(0,0); // Sets the location at which subsequent text written to the LCD will be displayed
lcd.print("Distance: "); // Prints string "Distance" on the LCD
lcd.print(distanceCm); // Prints the distance value from the sensor
lcd.print(" cm");
delay(10);
lcd.setCursor(0,1);
lcd.print("Distance: ");
Serial.println(distanceCm);
lcd.print(distanceInch);
lcd.print(" m");
delay(10);
}


#include <LiquidCrystal.h>

const int rs=12, en=11, d4=5, d5=4, d6=3, d7=2 ;
LiquidCrystal lcd(rs,en,d4,d5,d6,d7); // Creates an LCD object. Parameters: (rs, enable, d4, d5, d6, d7)
const int trigPin = 10;
const int echoPin = 13;
long duration;//LONG VARIABLE
float distance, distanceInch;
//declaration of all pin we used
 

void setup(){
lcd.begin(16,2); // Initializes the interface to the LCD screen, and specifies the dimensions (width and height) of the display
lcd.print("  WELCOME ATIQ  ");
lcd.setCursor(0,1);//FOR SECOND LINE OF LCD
lcd.print("FINAL PROJECT");
delay(5);
pinMode(trigPin, OUTPUT);
pinMode(echoPin, INPUT);
Serial.begin(9600);
}

void loop(){
digitalWrite(trigPin, LOW);
delay(2);
digitalWrite(trigPin, HIGH);
delay(10);
digitalWrite(trigPin, LOW);
duration = pulseIn(echoPin, HIGH);
distance = duration*340/20000;
lcd.setCursor(0,0); // Sets the location at which subsequent text written to the LCD will be displayed
lcd.print("Distance: "); // Prints string "Distance" on the LCD
lcd.print(distance); // Prints the distance value from the sensor
lcd.print(" cm");
delay(10);
lcd.setCursor(0,1);
lcd.print("Distance: ");
lcd.print(distance/100);
lcd.print(" m");
delay(10);
}




